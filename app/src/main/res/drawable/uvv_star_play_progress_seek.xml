<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />

            <gradient
                android:angle="-90"
                android:centerColor="@color/video_seek_background"
                android:centerY="0.75"
                android:endColor="@color/video_seek_background"
                android:startColor="@color/video_seek_background" />
        </shape>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="-90"
                    android:centerColor="#ffffffff"
                    android:centerY="0.75"
                    android:endColor="#ffffffff"
                    android:startColor="#ffffffff" />
            </shape>
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="-90"
                    android:centerColor="@color/normal_red"
                    android:centerY="0.75"
                    android:endColor="@color/normal_red"
                    android:startColor="@color/normal_red" />
            </shape>
        </clip>
    </item>

</layer-list>