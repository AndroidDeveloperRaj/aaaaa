<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_photofilm_pic"
    android:layout_width="@dimen/dp50"
    android:layout_height="@dimen/dp70"
    android:padding="@dimen/dp1">

    <ImageView
        android:id="@+id/iv_photofilm_pic"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/tv_photofilm_mobanlvjing_name"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp18"
        android:layout_alignParentBottom="true"
        android:background="@color/base_99222222"
        android:gravity="center"
        android:text="无"
        android:textColor="@drawable/selector_photofilmtab_moban_keai"
        android:textSize="@dimen/dp11" />
</RelativeLayout>